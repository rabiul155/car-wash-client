import { ServiceType } from '@/types/service';
import React from 'react';

type PropsType = {
  service: ServiceType | null;
};

function BookServiceForm(props: PropsType) {
  return <div>BookServiceForm</div>;
}

export default BookServiceForm;
